name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install ansible==6.0.0
          python -m pip install paramiko>=2.10

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}

      - name: Test SSH connection
        run: |
          echo "Testing SSH connection to ${{ secrets.HOST_IP }}"
          ssh -o StrictHostKeyChecking=no \
              -o ConnectTimeout=30 \
              ${{ secrets.ANSIBLE_USER }}@${{ secrets.HOST_IP }} \
              "echo 'SSH connection successful'"

      - name: Create inventory file
        run: |
          set -euo pipefail
          mkdir -p inventory_dir
          INVENTORY_FILE="inventory_dir/inventory"
          cat > "$INVENTORY_FILE" << EOF
          [webservers]
          ${{ secrets.HOST_IP }} ansible_ssh_common_args='-o StrictHostKeyChecking=no -o ConnectTimeout=30'
          
          [all:vars]
          ansible_user=${{ secrets.ANSIBLE_USER }}
          ansible_python_interpreter=/usr/bin/python3
          ansible_ssh_retries=3
          EOF
          echo "INVENTORY_PATH=$INVENTORY_FILE" >> $GITHUB_ENV

      - name: Run Ansible playbook
        env:
          ANSIBLE_HOST_KEY_CHECKING: "False"
          ANSIBLE_TIMEOUT: "60"
          ANSIBLE_SSH_RETRIES: "3"
        run: |
          set -euo pipefail
          ansible-playbook -i "${{ env.INVENTORY_PATH }}" \
            ansible-project/deploy.yml -v

      - name: Post-deployment verification
        if: success()
        run: |
          echo "Deployment successful!"
          ssh -o StrictHostKeyChecking=no \
              ${{ secrets.ANSIBLE_USER }}@${{ secrets.HOST_IP }} \
              "uptime && docker ps 2>/dev/null || echo 'Docker not available'"
